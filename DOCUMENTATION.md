# 📚 وثائق تطبيق مصحف المدينة

## نظرة عامة

تطبيق مصحف المدينة هو قارئ PDF مخصص للقرآن الكريم بخط المصحف المدني، مبني باستخدام Python و Qt6.

## الهيكل التقني

### المكتبات المستخدمة

| المكتبة | الاستخدام |
|---------|----------|
| PyQt6 | واجهة المستخدم الرسومية |
| PyMuPDF (fitz) | عرض ومعالجة PDF |

### هيكل الملفات

```
src/
├── main.py              # الكود الرئيسي
└── (ملفات إضافية)

assets/
└── icon.png             # أيقونة التطبيق

build-AppImage/
├── build.sh             # سكريبت بناء AppImage
├── AppDir/              # هيكل AppDir
└── README.md
```

## المكونات الرئيسية

### 1. PDFPageView

عارض صفحات PDF مخصص يدعم:
- التكبير/التصغير
- السحب والتمرير
- عرض جودة عالية

### 2. SurahListWidget

قائمة السور مع:
- البحث السريع
- التحديد التلقائي للسورة الحالية
- التنقل السريع

### 3. MushafViewer

النافذة الرئيسية التي تدير:
- عرض PDF
- التنقل بين الصفحات
- الإعدادات والتفضيلات

## الإعدادات

يتم حفظ الإعدادات في:
```
~/.config/mushaf-madinah/config.json
```

الإعدادات المحفوظة:
- `last_page`: آخر صفحة تم قراءتها
- `dark_mode`: الوضع الليلي/النهاري
- `zoom`: مستوى التكبير

## الاختصارات

| الاختصار | الوظيفة |
|---------|---------|
| `→` `↓` | الصفحة التالية |
| `←` `↑` | الصفحة السابقة |
| `Ctrl++` | تكبير |
| `Ctrl+-` | تصغير |
| `Ctrl+0` | إعادة التعيين |
| `Ctrl+F` | البحث |

## التخصيص

### تغيير الألوان

يمكن تعديل الألوان في دوال `_get_light_stylesheet()` و `_get_dark_stylesheet()`.

### إضافة خطوط

لإضافة خطوط عربية:
```python
font_db = QFontDatabase()
font_db.addApplicationFont("path/to/font.ttf")
```

## البناء والنشر

### بناء AppImage

```bash
cd build-AppImage
./build.sh
```

### بناء ثنائي مستقل

```bash
make build
```

## استكشاف الأخطاء

### مشكلة: التطبيق لا يبدأ

**الحل:**
```bash
# التحقق من المتطلبات
pip install -r requirements.txt

# تشغيل من الطرفية لرؤية الأخطاء
python src/main.py
```

### مشكلة: ملف PDF غير موجود

**الحل:**
ضع ملف `MushafMadinaHafsGreen1441HQ.pdf` في:
- مجلد التطبيق الرئيسي
- أو نفس مجلد `main.py`

### مشكلة: AppImage لا يعمل

**الحل:**
```bash
# تثبيت libfuse2
sudo apt-get install libfuse2

# أو استخراج وتشغيل
./Mushaf-Madinah-x86_64.AppImage --appimage-extract
./squashfs-root/AppRun
```

## المساهمة

انظر ملف [CONTRIBUTING.md](CONTRIBUTING.md) للتفاصيل.

## الترخيص

هذا المشروع مرخص بموجب MIT License.
